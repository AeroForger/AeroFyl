# Modules and projects

Module syntax, module lookup, imported-name behavior, and the grammar of `use` and `using` are not specified yet.

TOML is recorded as the intended project configuration format. The manifest file name and key schema are not specified yet. Package management is not specified yet.
