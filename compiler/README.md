# Aerofyl Alpha compiler

This directory is reserved for the first compiler written manually in Aerofyl. It is not implemented yet.

The `frontend/` directory contains only an initial token data model that the Rust bootstrap can check. Lexer, parser, AST, HIR, IR, backend, and driver implementations are TODOs for Alpha 1.
